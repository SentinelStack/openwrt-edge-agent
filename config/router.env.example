# Copy this file to config/router.env and edit values locally.
# Do not commit config/router.env.

# Router SSH target
ROUTER=root@192.168.1.1

# Auth mode: key (recommended) or password
ROUTER_AUTH_MODE=key

# Used only when ROUTER_AUTH_MODE=password
# ROUTER_PASSWORD=change-me
