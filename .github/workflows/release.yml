name: OpenWrt Agent Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build ARM Binary and Publish Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: make docker-build

      - name: Cross-compile net_filter
        run: make build

      - name: Validate binary type
        run: make check

      - name: Prepare release assets
        run: |
          cp net_filter net_filter-armv7
          sha256sum net_filter-armv7 > net_filter-armv7.sha256

      - name: Create or update GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            net_filter-armv7
            net_filter-armv7.sha256
